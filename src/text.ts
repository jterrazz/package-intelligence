/**
 * Dependency-free text utilities entry point.
 *
 * Import from `@jterrazz/intelligence/text` to use text parsing without
 * installing the optional `ai` and `zod` peer dependencies.
 */
export { parseText, type ParseTextOptions } from './parsing/parse-text.js';
